<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="GitRecommender.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:GitRecommender"
    Shell.FlyoutBehavior="Flyout">

    <!--<ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage"/>-->


      <FlyoutItem Title="Main">
        <ShellContent
            Title="Home"
            ContentTemplate="{DataTemplate local:MainPage}"
            Route="MainPage" />
    </FlyoutItem>

    <FlyoutItem Title="Welcome">
        <ShellContent
            Title="Welcome"
            ContentTemplate="{DataTemplate local:Welcome}"
            Route="Welcome" />
    </FlyoutItem>
</Shell>

